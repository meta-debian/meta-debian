# qemumips assumes that big-endian is the default endian
CONFIG_CPU_BIG_ENDIAN=y

# The default system assumes that kernel automatically
# required device files by mounting devtempfs.
CONFIG_DEVTMPFS_MOUNT=y
