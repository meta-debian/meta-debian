# zImage needs to have DTB data by itself if zImage is used as kernel.img
CONFIG_ARM_APPENDED_DTB=y
# get the ATAG information from a bootloader and put it into the appended DTB
CONFIG_ARM_ATAG_DTB_COMPAT=y
