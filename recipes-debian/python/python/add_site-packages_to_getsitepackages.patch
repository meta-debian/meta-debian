Debian sets sitepackages to "/usr/lib/python<version>/dist-packages"
by debian/patches/site-locations.diff,
but meta layer uses "/usr/lib/python<version>/site-packages"
which is declared in meta/classes/python-dir.bbclass.

This makes a lot of python modules are installed to site-packages,
but python looks for them in dist-packages.

Append "/usr/lib/python<version>/site-packages" to sitepackages
to avoid error: "ImportError: No module named ..."

diff --git a/Lib/site.py b/Lib/site.py
index 445f78c..31b9524 100644
--- a/Lib/site.py
+++ b/Lib/site.py
@@ -304,13 +304,22 @@ def getsitepackages():
             sitepackages.append(os.path.join(prefix, "local/lib",
                                         "python" + sys.version[:3],
                                         "dist-packages"))
+            sitepackages.append(os.path.join(prefix, "local/lib",
+                                        "python" + sys.version[:3],
+                                        "site-packages"))
             sitepackages.append(os.path.join(prefix, sys.lib,
                                         "python" + sys.version[:3],
                                         "dist-packages"))
+            sitepackages.append(os.path.join(prefix, sys.lib,
+                                        "python" + sys.version[:3],
+                                        "site-packages"))
             if sys.lib != "lib":
                 sitepackages.append(os.path.join(prefix, "lib",
                                         "python" + sys.version[:3],
                                         "dist-packages"))
+                sitepackages.append(os.path.join(prefix, "lib",
+                                        "python" + sys.version[:3],
+                                        "site-packages"))
         else:
             sitepackages.append(prefix)
             sitepackages.append(os.path.join(prefix, sys.lib, "site-packages"))
